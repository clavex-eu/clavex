# Binaries
bin/
*.exe
*.dll
*.so
*.dylib

# Build output
build/frontend/assets/

# Go test cache / coverage
*.test
*.out
coverage.html

# Config with secrets (keep example)
config.dev.yaml
config.prod.yaml
config.*.yaml
!config.example.yaml

# Keys & certs
keys/*.pem

# FAPI2 conformance test keypair (generated by make conformance-setup, contains private key)
conformance/fapi-private-key.jwk
keys/*.key
keys/*.pub
keys/*.crt
*.p12

# Logos uploaded at runtime
logos/*
!logos/.gitkeep

# Env files
.env
.env.*
!.env.example

# Terraform state
terraform-provider/.terraform/
terraform-provider/terraform.tfstate*
terraform-provider/*.tfvars

# Node / frontend
frontend/node_modules/
sdk/node/node_modules/
sdk/react/node_modules/
sdk/nextauth-provider/node_modules/
sdk/webhooks-js/node_modules/
frontend/dist/

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Scratch files at workspace root
ql\ *
tgres*
et
.conformance-suite

# Marketplace operator — closed-source, not published in this repo
cmd/marketplace-operator/
marketplace-operator

# Compiled binaries at workspace root
regulatory-monitor

.agents/
.claude/
skills-lock.json
