# Base URL of the clavex API server (no trailing slash, no /api/v1 suffix).
# Leave empty in development — Vite proxy forwards /api requests to localhost:8080.
# In production set this to the public API endpoint, e.g.:
#   VITE_API_URL=https://api.clavex.example.com
VITE_API_URL=
